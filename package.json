{
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "gh-pages -d dist"
},
  "dependencies": {
    "three": "^0.151.3"
  },
  "devDependencies": {
    "vite": "^4.3.1"
  }
  
}

